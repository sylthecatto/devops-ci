PORT=3000

PGHOST=localhost
PGPORT=5432
PGUSER=appuser
PGPASSWORD=apppassword
PGDATABASE=appdb

SESSION_SECRET=change-me-in-production

# Injected by Jenkins/Kubernetes at build/deploy time; defaults are used
# when running locally.
BUILD_VERSION=dev
ENVIRONMENT=local
